import WTW from './WTW.js';

describe('test WTW lib ', () => {
    it('passes', () => {
        expect(WTW('passes').ok).toBe(true);
    });
    it('fails', () => {
        expect(WTW('fails')?.ok!).toBe(false);
    });
})
