{
  "name": "haibun",
  "devDependencies": {
    "@types/node": "^18.11.9",
    "nodemon": "^2.0.20",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "test": "lerna run test",
    "test-watch": "nodemon -x 'npm run test'",
    "build": "lerna clean -y && lerna bootstrap && bin/setup-links.sh",
    "tsc": "tsc -b modules/",
    "tsc-watch": "tsc -b modules/ --watch"
  },
  "dependencies": {
    "@types/jest": "^29.2.2"
  }
}
