Feature: A group that another feature runs

This group exists to be started by a feature of the general group, which states that a group runs once per state of what it depends on. It holds one feature, and that feature passes.
